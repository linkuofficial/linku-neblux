# TASK: p2-1-reflect（P2-1 reflect 內容補完）
狀態: done

## 目標

為現有 19 趟 Wonders tour 補上 `reflect` 欄位，讓 P2-1 旅程紀錄列印頁不再只 fallback 到 `outward`，而是提供每趟 2 題三語反思問題。

## 驗收條件
- [x] 19 個 `data/wonders/*.json` 皆有 `reflect.en` / `reflect.zh` / `reflect.ja`。
- [x] 每語 2 題，皆為開放式問句，不是事實測驗題。
- [x] zh 使用繁體中文；三語不是逐句硬翻，而是保留同一思考方向的在地化改寫。
- [x] 不碰 P1-3 telemetry 範圍：`functions/`、telemetry flag、`frontend/src/wonders-main.js`、telemetry tests。
- [x] `npm run verify` 全過；`python scripts/check_simplified.py` 0 ERROR。

## 邊界（不要動的東西）

- 不改 tour schema 機制、不改前端 rendering、不改 P2-1 列印頁程式。
- 不改 `frontend/src/engine/`、部署設定、`functions/`。
- 不碰 `docs/tasks/2026-07-08-p1-3-telemetry.md` 內容，避免和 Claude 的 P1-3 brief 撞車。

## Questions（Codex 填）

- 無。Riku 已交派 `P2-1 reflect`，且 P1-3 brief 明確把 `data/wonders/*.json` 留給本任務。

## HANDOFF（Codex 完成或卡住後填）
- Branch: `master`（沿用現有工作樹；未 commit）
- Summary:
  - 19 個 `data/wonders/*.json` 全部補上 `reflect`。
  - 每趟三語各 2 題，插在 `outward` 後、`outward_links` 前（若有）。
  - 未修改 P1-3 telemetry 檔案；P1-3 完成後目前工作樹只剩本任務的 tour JSON 與 brief。
- Verification:
  - JSON parse + reflect count：19 趟皆 `en/zh/ja = 2/2/2`。
  - reflect 問句結尾檢查：OK。
  - `python scripts/check_simplified.py`：0 ERROR。
  - `python scripts/check_i18n_chars.py`：RESULT OK。
  - `npm run verify`：build 成功，e2e 57 passed。
- Remaining risks:
  - ja reflect 自然度仍建議日後母語者過目。
