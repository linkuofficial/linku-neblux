neblux.linku.tech {
    encode zstd gzip

    reverse_proxy localhost:8000

    header {
        X-Content-Type-Options nosniff
        X-Frame-Options SAMEORIGIN
        Referrer-Policy strict-origin-when-cross-origin
    }
}

www.neblux.linku.tech {
    redir https://neblux.linku.tech{uri} permanent
}
