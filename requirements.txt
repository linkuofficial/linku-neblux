# Nexus Knowledge Graph — Dependencies
# Core
anthropic>=0.30.0
pyyaml>=6.0

# Backend API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0

# HTTP client (testing, external calls)
httpx>=0.27.0

# Deduplication (optional, for semantic mode)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Vector store (embedding persistence)
chromadb>=0.5.0

# Database
neo4j>=5.0.0

# Testing
pytest>=7.0.0
httpx>=0.27.0
